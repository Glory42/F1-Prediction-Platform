services:
  - type: web
    name: f1-data-engine
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m src.server
    envVars:
      - key: DATABASE_URL
        sync: false
